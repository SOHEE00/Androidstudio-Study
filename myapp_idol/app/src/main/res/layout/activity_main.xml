<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <!-- 아이돌 그룹 이미지 -->
    <ImageView
        android:id="@+id/groupImage"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginBottom="20dp"
        android:src="@drawable/skz_image" />

    <!-- 아이돌 그룹 이름 -->
    <TextView
        android:id="@+id/groupName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stray Kids"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <!-- 아이돌 그룹 정보 버튼 -->
    <Button
        android:id="@+id/infoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SKZ 정보 보기"
        android:layout_marginBottom="16dp"/>

    <!-- 계산기 입력 칸 -->
    <EditText
        android:id="@+id/calculatorInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="멤버 이름을 입력하세요"
        android:inputType="number" />

    <!-- 계산기 버튼 -->
    <Button
        android:id="@+id/calculateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="투표"
        android:layout_marginBottom="16dp"/>

    <!-- 계산 결과 출력 -->
    <TextView
        android:id="@+id/calculatorResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="16sp"/>

    <!-- 투표한 멤버의 사진 (여기에 해당 멤버의 사진을 표시) -->
    <ImageView
        android:id="@+id/memberImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="16dp"
        android:visibility="gone" /> <!-- 기본적으로 이미지 숨기기 -->

</LinearLayout>